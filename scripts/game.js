function Game(statusGame, board, user, currentTurn, move, timer, level) {
 this.statusGame = statusGame;
 this.board = board;
 this.user = user;
 this.currentTurn = currentTurn;
 this.move = move;
 this.timer = timer;
 this.level = level;
 function startGame() {

 }
 function endGame() {

 }
 function playerMove(player, startX, startY, endX, endY) {
  
 }
}